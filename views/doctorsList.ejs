<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Find Doctors</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: #f5f5f5;
            padding: 20px;
        }
        .header {
            background: #333;
            color: white;
            padding: 20px;
            margin-bottom: 30px;
        }
        .header h1 {
            margin: 0;
        }
        .back-link {
            color: white;
            text-decoration: none;
            margin-right: 20px;
        }
        .back-link:hover {
            text-decoration: underline;
        }
        .container {
            max-width: 1200px;
            margin: 0 auto;
        }
        .doctors-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 20px;
        }
        .doctor-card {
            background: white;
            border: 1px solid #ddd;
            padding: 20px;
            border-radius: 5px;
        }
        .doctor-header {
            display: flex;
            align-items: center;
            margin-bottom: 15px;
        }
        .doctor-avatar {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            background: #eee;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 40px;
            margin-right: 15px;
            overflow: hidden;
        }
        .doctor-avatar img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
        .doctor-info h3 {
            margin: 0;
            color: #333;
        }
        .specialty {
            color: #666;
            font-size: 14px;
            margin: 5px 0;
        }
        .consult-btn {
            width: 100%;
            padding: 10px;
            background: #333;
            color: white;
            border: none;
            cursor: pointer;
            margin-top: 15px;
        }
        .consult-btn:hover {
            background: #555;
        }
        .no-doctors {
            text-align: center;
            padding: 40px;
            color: #666;
        }
    </style>
</head>
<body>
    <div class="header">
        <a href="/patientDashboard" class="back-link">← Back to Dashboard</a>
        <h1>Find Doctors</h1>
    </div>

    <div class="container">
        <% if (doctors && doctors.length > 0) { %>
            <div class="doctors-grid">
                <% doctors.forEach(doctor => { %>
                    <div class="doctor-card">
                        <div class="doctor-header">
                            <div class="doctor-avatar">
                                <% if (doctor.profilePicture) { %>
                                    <img src="<%= doctor.profilePicture %>" alt="<%= doctor.name %>">
                                <% } else { %>
                                    👨‍⚕️
                                <% } %>
                            </div>
                            <div class="doctor-info">
                                <h3><%= doctor.name %></h3>
                                <div class="specialty"><%= doctor.specialty %></div>
                            </div>
                        </div>
                        <form action="/consultation/<%= doctor.id %>" method="get">
                            <button type="submit" class="consult-btn">Consult</button>
                        </form>
                    </div>
                <% }); %>
            </div>
        <% } else { %>
            <div class="no-doctors">
                <p>No doctors available at the moment.</p>
            </div>
        <% } %>
    </div>
</body>
</html>
